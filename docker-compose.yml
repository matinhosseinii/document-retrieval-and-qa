services:
  web:
    build: .
    command: sh -c "python manage.py migrate && exec gunicorn --bind 0.0.0.0:8000 --workers 1 config.wsgi:application"
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
      - ./data/chroma:/app/data/chroma
